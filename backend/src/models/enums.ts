export enum PaymentMethod {
  QRIS = 'QRIS',
  BANK_TRANSFER = 'BANK_TRANSFER',
}

export enum CrowdfundStatus {
  OPEN = 'OPEN',
  CLOSE = 'CLOSE',
}

export enum UserRole {
  ADMIN = 'ADMIN',
  USER = 'USER',
}